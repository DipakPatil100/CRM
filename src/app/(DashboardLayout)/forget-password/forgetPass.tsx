
import Box from "@mui/material/Box";
import styled from "styled-components";

export const EditFlex = styled(Box)({
  display: "flex",
      justifyContent:" space-between",
      cursor: "pointer"
  });

  export const NewPassword = styled(Box)({
    textAlign: "start",
       marginBottom: "4px",
       display: "flex"
  })